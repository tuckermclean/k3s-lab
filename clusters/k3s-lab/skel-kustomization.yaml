apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: skel
  namespace: flux-system
spec:
  interval: 10m
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: ./apps/skel
  prune: true
  # If your skel/app requires Traefik (Ingress) or cert-manager, add dependencies here:
  dependsOn: []

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # `flux bootstrap` (run by bootstrap/terraform/ovh-k3s) populates flux-system/.
  # Add infrastructure/app Flux Kustomizations here the same way clusters/k3s-lab does.
  - flux-system/

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# This cluster is not bootstrapped yet — flux-system/ does not exist, so resources
# is empty to keep `kustomize build clusters/oci-lab` clean instead of erroring.
# Once `flux bootstrap` (run by bootstrap/terraform/oci-k3s) populates flux-system/,
# add it (and infrastructure/app Flux Kustomizations) here the way clusters/ovh-lab does:
#   - flux-system/
resources: []

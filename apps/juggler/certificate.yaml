apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: juggler-cert
  namespace: juggler
spec:
  secretName: juggler-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod
  dnsNames:
    - juggler.dcxxiv.com

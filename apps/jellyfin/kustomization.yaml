apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - deployment.yaml
  - media-volume.yaml
  - service.yaml
  - media-ingress.yaml
# Explicitly choosing plain manifests for Jellyfin; the HelmRelease present here is unused and will be removed to avoid confusion.

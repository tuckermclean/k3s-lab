apiVersion: traefik.io/v1alpha1
kind: IngressRouteUDP
metadata:
  name: bedrock-udp
  namespace: minecraft
spec:
  entryPoints:
    - bedrock   # define in Traefik as address: ":19132/udp"
  routes:
    - services:
        - name: bedrock
          port: 19132

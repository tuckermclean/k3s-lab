apiVersion: apps/v1
kind: Deployment
metadata:
  name: personliness
  namespace: personliness
  labels:
    app.kubernetes.io/name: personliness
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: personliness
  template:
    metadata:
      labels:
        app.kubernetes.io/name: personliness
    spec:
      containers:
        - name: web
          image: ghcr.io/tuckermclean/personliness/web:latest
          imagePullPolicy: Always
          command: ["/entrypoint.sh"]
          envFrom:
            - configMapRef:
                name: personliness-config
            - secretRef:
                name: personliness-secret
          volumeMounts:
            - name: media-files
              mountPath: /app/media
            - name: static-files
              mountPath: /app/staticfiles

        - name: worker
          image: ghcr.io/tuckermclean/personliness/web:latest
          imagePullPolicy: Always
          command: ["celery", "-A", "personliness", "worker", "--loglevel=info"]
          envFrom:
            - configMapRef:
                name: personliness-config
            - secretRef:
                name: personliness-secret
          volumeMounts:
            - name: media-files
              mountPath: /app/media

        - name: frontend
          image: ghcr.io/tuckermclean/personliness/frontend:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 80
          volumeMounts:
            - name: media-files
              mountPath: /app/media
              readOnly: true
            - name: static-files
              mountPath: /app/staticfiles
              readOnly: true
            - name: nginx-config
              mountPath: /etc/nginx/conf.d/default.conf
              subPath: nginx.conf

      volumes:
        - name: media-files
          persistentVolumeClaim:
            claimName: media-files
        - name: static-files
          emptyDir: {}
        - name: nginx-config
          configMap:
            name: personliness-config
---
apiVersion: v1
kind: Service
metadata:
  name: personliness
  namespace: personliness
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: personliness
  ports:
    - name: http
      port: 80
      targetPort: 80

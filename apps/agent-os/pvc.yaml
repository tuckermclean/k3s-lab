apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: agent-os-home
  namespace: agent-os
  labels:
    app.kubernetes.io/part-of: gitops
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  storageClassName: longhorn

apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: agent-os-cert
  namespace: agent-os
spec:
  secretName: agent-os-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod
  dnsNames:
    - agent.dcxxiv.com

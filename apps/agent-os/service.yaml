apiVersion: v1
kind: Service
metadata:
  name: agent-os
  namespace: agent-os
  labels:
    app.kubernetes.io/name: agent-os
    app.kubernetes.io/part-of: gitops
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: agent-os
  ports:
    - name: http
      port: 3011
      targetPort: 3011

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: flaresolverr-ingress
  namespace: media
  annotations:
    cert-manager.io/cluster-issuer: "local-selfsigned"
  labels:
    app.kubernetes.io/part-of: gitops
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - flaresolverr.home.dcxxiv.com
      secretName: flaresolverr-tls
  rules:
    - host: flaresolverr.home.dcxxiv.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: flaresolverr
                port:
                  number: 8191

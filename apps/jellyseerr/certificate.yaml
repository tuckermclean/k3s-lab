apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: jellyseerr-cert
  namespace: media
spec:
  secretName: jellyseerr-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod
  dnsNames:
    - jellyseerr.home.dcxxiv.com

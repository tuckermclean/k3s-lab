# Chart source: the orchestrator Helm chart lives in an external public repo
# at charts/orchestrator/. Pinned to the v0.1.1 release tag.
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: orchestrator
  namespace: flux-system
spec:
  interval: 1h
  url: https://github.com/tuckermclean/orchestrator
  ref:
    tag: v0.1.9

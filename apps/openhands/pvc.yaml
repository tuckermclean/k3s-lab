apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: openhands-pvc
  namespace: openhands
  labels:
    app.kubernetes.io/part-of: gitops
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 50Gi
  storageClassName: local-path

apiVersion: v1
kind: Service
metadata:
  name: openhands
  namespace: openhands
  labels:
    app.kubernetes.io/part-of: gitops
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: openhands
  ports:
    - name: http
      port: 3000
      targetPort: 3000

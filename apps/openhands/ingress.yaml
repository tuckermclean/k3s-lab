apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: openhands-ingress
  namespace: openhands
  annotations:
    kubernetes.io/ingress.class: "traefik"
    cert-manager.io/cluster-issuer: "local-selfsigned"
    traefik.ingress.kubernetes.io/router.middlewares: traefik-forward-auth@kubernetescrd
    traefik.ingress.kubernetes.io/router.middlewares-https: traefik-forward-auth@kubernetescrd
  labels:
    app.kubernetes.io/part-of: gitops
spec:
  tls:
    - hosts:
        - openhands.home.dcxxiv.com
      secretName: openhands-tls
  rules:
    - host: openhands.home.dcxxiv.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: openhands
                port:
                  number: 3000

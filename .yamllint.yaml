extends: default

ignore: |
  clusters/*/flux-system/
  # Vendored upstream manifest (kubernetes-sigs/agent-sandbox) — not our style.
  infrastructure/agent-sandbox/sandbox.yaml

rules:
  document-start: disable
  truthy: disable
  line-length: disable
  new-line-at-end-of-file: disable
  comments: disable
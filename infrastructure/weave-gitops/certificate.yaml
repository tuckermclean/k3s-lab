apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: weave-gitops-tls
  namespace: weave-gitops
spec:
  secretName: weave-gitops-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod
  dnsNames:
    - flux.home.dcxxiv.com

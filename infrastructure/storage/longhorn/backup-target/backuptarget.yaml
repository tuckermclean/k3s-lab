apiVersion: longhorn.io/v1beta2
kind: BackupTarget
metadata:
  name: default
  namespace: longhorn-system
spec:
  backupTargetURL: ${BACKUP_TARGET}
  credentialSecret: longhorn-backup-secret
  pollInterval: 5m0s

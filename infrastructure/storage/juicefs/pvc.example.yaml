apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: juicefs-app-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: juicefs-sc
  resources:
    requests:
      storage: 100Gi

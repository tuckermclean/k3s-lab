apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: juicefs-app-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: juicefs
  resources:
    requests:
      storage: 100Gi

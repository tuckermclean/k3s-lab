apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: juicefs
provisioner: csi.juicefs.com
reclaimPolicy: Delete
allowVolumeExpansion: true
volumeBindingMode: Immediate
mountOptions:
  - enable-xattr
  # Cap the local read cache so /var/jfsCache can't fill the node disk.
  # JuiceFS CE defaults to a 100 GiB cache + 10% free-space floor — on the
  # 48 GiB OVH nodes that grew /var/jfsCache to ~29 GiB and tripped
  # DiskPressure. 10 GiB cache + a 20% free-space floor keeps headroom.
  - cache-size=10240
  - free-space-ratio=0.2
parameters:
  csi.storage.k8s.io/provisioner-secret-name: juicefs-secret
  csi.storage.k8s.io/provisioner-secret-namespace: kube-system
  csi.storage.k8s.io/node-publish-secret-name: juicefs-secret
  csi.storage.k8s.io/node-publish-secret-namespace: kube-system
  csi.storage.k8s.io/controller-expand-secret-name: juicefs-secret
  csi.storage.k8s.io/controller-expand-secret-namespace: kube-system
  # For CE with S3 backend, define required keys in the Secret (name, metaurl, bucket, access-key, secret-key, region)
  # Additional options can be specified in Secret's format-options

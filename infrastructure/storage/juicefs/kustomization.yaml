apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../redis-juicefs/
  - helmrepository.yaml
  - helmrelease.yaml
  # NOTE: Secrets are not committed. Create your own secret based on secret-juicefs-aws.example.yaml
  # - secret-juicefs-aws.yaml
  - pvc.yaml
  - storageclass.yaml